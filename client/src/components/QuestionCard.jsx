import { motion } from 'framer-motion'
export default function QuestionCard({ question, selected, onSelect, number }) { return <motion.article initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="glass rounded-2xl p-6 md:p-8"><p className="text-sm font-semibold text-cyan-300">Question {number} · {question.marks} mark{question.marks !== 1 ? 's' : ''}</p><h2 className="mt-3 text-xl font-semibold leading-relaxed md:text-2xl">{question.question}</h2><div className="mt-7 space-y-3">{question.options.map((option,index) => <button key={option.id} onClick={() => onSelect(option.id)} className={`flex w-full items-center gap-4 rounded-xl border p-4 text-left text-sm transition ${selected === option.id ? 'border-cyan-400 bg-cyan-400/10 text-white' : 'border-white/10 bg-white/[.03] text-zinc-300 hover:border-violet-400/50 hover:bg-white/[.06]'}`}><span className="grid size-7 shrink-0 place-items-center rounded-lg border border-current text-xs font-bold">{String.fromCharCode(65+index)}</span>{option.text}</button>)}</div></motion.article> }
