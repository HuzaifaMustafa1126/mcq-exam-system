import { useQuery } from '@tanstack/react-query'
import { CheckCircle2, Printer, XCircle } from 'lucide-react'
import Button from '../Button'
import Modal from '../Modal'
import Skeleton from '../Skeleton'
import { getResult } from '../../services/results'

export default function ResultDetailsModal({ attemptId, onClose }) { const { data, isPending, isError } = useQuery({ queryKey: ['result', attemptId], queryFn: () => getResult(attemptId), enabled: Boolean(attemptId) }); return <Modal open={Boolean(attemptId)} onClose={onClose} title="Detailed result">{isPending && <div className="space-y-3"><Skeleton className="h-18" /><Skeleton className="h-14" /><Skeleton className="h-14" /></div>}{isError && <p className="rounded-xl bg-rose-500/10 p-4 text-sm text-rose-300">Unable to load the result.</p>}{data && <div className="max-h-[70vh] overflow-y-auto pr-1"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold">{data.studentName}</p><p className="text-sm text-zinc-500">Student no: {data.studentNumber || '—'} · {data.examName}</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${data.status === 'PASS' ? 'bg-emerald-500/10 text-emerald-300' : 'bg-rose-500/10 text-rose-300'}`}>{data.status}</span></div><div className="mt-4 grid grid-cols-3 gap-3 text-center"><Metric label="Score" value={`${data.obtainedMarks}/${data.totalMarks}`} /><Metric label="Percentage" value={`${data.percentage}%`} /><Metric label="Correct" value={data.correct} /></div><div className="mt-5 space-y-2">{data.answers.map((answer, index) => <div key={index} className={`rounded-xl border p-3 ${answer.isCorrect ? 'border-emerald-500/20 bg-emerald-500/5' : 'border-white/10 bg-white/[.03]'}`}><div className="flex gap-2"><span className={answer.isCorrect ? 'text-emerald-300' : 'text-rose-300'}>{answer.isCorrect ? <CheckCircle2 size={17} /> : <XCircle size={17} />}</span><p className="text-sm text-zinc-200">{index + 1}. {answer.questionText}</p></div><p className="mt-2 text-xs text-zinc-500">Selected: {answer.selectedOption || 'Unanswered'} · Correct: {answer.correctOption} · Marks awarded: {answer.marksAwarded}</p></div>)}</div><Button className="mt-5 w-full print:hidden" onClick={() => window.print()}><Printer size={16} />Print / save as PDF</Button></div>}</Modal> }
function Metric({ label, value }) { return <div className="rounded-xl bg-white/[.03] p-3"><p className="font-semibold">{value}</p><p className="mt-1 text-xs text-zinc-500">{label}</p></div> }
