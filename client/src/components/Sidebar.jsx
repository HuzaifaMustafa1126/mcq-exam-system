import { BarChart3, BookOpen, FileText, LayoutDashboard, LogOut, Settings, Users, HelpCircle } from 'lucide-react'
import { NavLink, useNavigate } from 'react-router-dom'
import { useAuth } from '../hooks/useAuth'
const studentItems = [{ to:'/dashboard', label:'Dashboard', icon:LayoutDashboard }, { to:'/exams', label:'Exams', icon:BookOpen }, { to:'/result/1', label:'Results', icon:BarChart3 }, { to:'/dashboard', label:'Profile', icon:Users }]
const adminItems = [{ to:'/admin', label:'Dashboard', icon:LayoutDashboard }, { to:'/admin/teachers', label:'Teachers', icon:Users }, { to:'/admin/students', label:'Students', icon:Users }, { to:'/admin/subjects', label:'Subjects', icon:BookOpen }, { to:'/admin/exams', label:'Exams', icon:BookOpen }, { to:'/admin/questions', label:'Questions', icon:HelpCircle }, { to:'/admin', label:'Results', icon:FileText }, { to:'/admin', label:'Settings', icon:Settings }]
export default function Sidebar({ admin = false, fixed = false }) { const { logout, user } = useAuth(); const navigate = useNavigate(); const items = admin ? adminItems : studentItems; return <aside className={`glass hidden w-64 shrink-0 flex-col border-y-0 border-l-0 p-5 lg:flex ${fixed ? 'lg:fixed lg:inset-y-0 lg:left-0 lg:z-30' : ''}`}><NavLink to={admin?'/admin':'/dashboard'} className="mb-10 flex items-center gap-2 text-xl font-bold"><span className="grid size-9 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-cyan-400">E</span>Examora</NavLink><div className="space-y-1">{items.map(({to,label,icon:Icon}) => <NavLink key={label} to={to} className={({isActive}) => `flex items-center gap-3 rounded-xl px-3 py-3 text-sm ${isActive ? 'bg-gradient-to-r from-violet-500/25 to-cyan-400/10 text-white' : 'text-zinc-400 hover:bg-white/5 hover:text-white'}`}><Icon size={18}/>{label}</NavLink>)}</div><div className="mt-auto"><div className="mb-4 rounded-xl border border-white/10 bg-white/5 p-3 text-xs text-zinc-400"><p className="font-semibold text-white">{user?.name || 'Exam learner'}</p><p className="mt-1 capitalize">{user?.role}</p></div><button onClick={() => { logout(); navigate('/login') }} className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm text-zinc-400 hover:bg-rose-500/10 hover:text-rose-300"><LogOut size={18}/>Logout</button></div></aside> }
