import { useQuery } from '@tanstack/react-query'
import { CheckCircle2 } from 'lucide-react'
import Modal from '../Modal'
import Skeleton from '../Skeleton'
import { getQuestion } from '../../services/questions'

export default function QuestionPreviewModal({ questionId, onClose }) { const { data, isPending, isError } = useQuery({ queryKey: ['question', questionId], queryFn: () => getQuestion(questionId), enabled: Boolean(questionId) }); return <Modal open={Boolean(questionId)} onClose={onClose} title="Question preview">{isPending && <div className="space-y-3"><Skeleton className="h-20" /><Skeleton className="h-12" /><Skeleton className="h-12" /></div>}{isError && <p className="rounded-xl bg-rose-500/10 p-4 text-sm text-rose-300">Unable to load this question.</p>}{data && <div><div className="flex flex-wrap gap-2 text-xs"><span className="rounded-full bg-cyan-400/10 px-2.5 py-1 text-cyan-300">{data.subjectName}</span><span className="rounded-full bg-white/5 px-2.5 py-1 capitalize text-zinc-400">{data.difficulty}</span><span className="rounded-full bg-white/5 px-2.5 py-1 text-zinc-400">{data.marks} marks</span></div><p className="mt-4 text-base font-medium leading-7 text-white">{data.questionText}</p><div className="mt-5 space-y-2">{data.options.map((option, index) => <div key={option.id} className={`flex items-center gap-3 rounded-xl border p-3 text-sm ${option.isCorrect ? 'border-emerald-500/30 bg-emerald-500/10 text-emerald-200' : 'border-white/10 bg-white/[.03] text-zinc-300'}`}><span className="grid size-6 shrink-0 place-items-center rounded-full bg-white/10 text-xs">{String.fromCharCode(65 + index)}</span>{option.optionText}{option.isCorrect && <CheckCircle2 className="ml-auto text-emerald-300" size={17} />}</div>)}</div></div>}</Modal> }
