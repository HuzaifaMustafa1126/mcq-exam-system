import { useQuery } from '@tanstack/react-query'
import { Clock3, ListChecks } from 'lucide-react'
import Modal from '../Modal'
import Skeleton from '../Skeleton'
import { getExam, getExamQuestions } from '../../services/exams'

export default function ExamPreviewModal({ examId, onClose }) { const exam = useQuery({ queryKey: ['exam', examId], queryFn: () => getExam(examId), enabled: Boolean(examId) }); const questions = useQuery({ queryKey: ['exam-questions', examId], queryFn: () => getExamQuestions(examId), enabled: Boolean(examId) }); return <Modal open={Boolean(examId)} onClose={onClose} title="Exam preview">{(exam.isPending || questions.isPending) && <div className="space-y-3"><Skeleton className="h-20" /><Skeleton className="h-16" /><Skeleton className="h-16" /></div>}{(exam.isError || questions.isError) && <p className="rounded-xl bg-rose-500/10 p-4 text-sm text-rose-300">Unable to load exam preview.</p>}{exam.data && questions.data && <div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-cyan-400/10 px-2.5 py-1 text-xs text-cyan-300">{exam.data.subjectName}</span><span className="rounded-full bg-white/5 px-2.5 py-1 text-xs capitalize text-zinc-400">{exam.data.status}</span></div><h3 className="mt-4 text-lg font-bold">{exam.data.title}</h3><p className="mt-2 text-sm text-zinc-400">{exam.data.description || 'No description provided.'}</p><div className="mt-5 grid grid-cols-3 gap-3"><Metric icon={Clock3} label="Duration" value={`${exam.data.durationMinutes} min`} /><Metric icon={ListChecks} label="Questions" value={questions.data.totalAssignedQuestions} /><Metric label="Total marks" value={exam.data.totalMarks} /></div><div className="mt-5 space-y-2">{questions.data.questions.map((question, index) => <div key={question.id} className="rounded-xl border border-white/10 bg-white/[.03] p-3"><div className="flex justify-between gap-3"><p className="text-sm text-zinc-200">{index + 1}. {question.questionText}</p><span className="shrink-0 text-xs text-cyan-300">{question.marks} marks</span></div></div>)}</div></div>}</Modal> }
function Metric({ icon: Icon, label, value }) { return <div className="rounded-xl bg-white/[.03] p-3 text-center">{Icon && <Icon className="mx-auto text-cyan-300" size={16} />}<p className="mt-1 text-sm font-semibold">{value}</p><p className="text-xs text-zinc-500">{label}</p></div> }
