import { motion } from 'framer-motion'
import clsx from 'clsx'
export default function Button({ children, className, variant = 'primary', ...props }) { return <motion.button whileHover={{ y: -2 }} whileTap={{ scale: .98 }} className={clsx('inline-flex items-center justify-center gap-2 rounded-xl px-5 py-3 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-60', variant === 'primary' ? 'bg-gradient-to-r from-violet-500 via-blue-500 to-cyan-400 text-white shadow-lg shadow-violet-500/25' : 'border border-white/10 bg-white/5 text-white hover:bg-white/10', className)} {...props}>{children}</motion.button> }
