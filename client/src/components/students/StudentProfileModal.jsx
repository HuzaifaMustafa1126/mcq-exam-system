import { useQuery } from '@tanstack/react-query'
import { Mail, Phone, UserRound } from 'lucide-react'
import Modal from '../Modal'
import Skeleton from '../Skeleton'
import { getStudent } from '../../services/students'

export default function StudentProfileModal({ studentId, onClose }) { const { data, isPending, isError } = useQuery({ queryKey: ['student', studentId], queryFn: () => getStudent(studentId), enabled: Boolean(studentId) }); return <Modal open={Boolean(studentId)} onClose={onClose} title="Student profile">{isPending && <div className="space-y-4"><Skeleton className="h-14" /><Skeleton className="h-14" /><Skeleton className="h-14" /></div>}{isError && <p className="rounded-xl bg-rose-500/10 p-4 text-sm text-rose-300">Unable to load this student.</p>}{data && <div className="space-y-3"><div className="flex items-center gap-3 pb-2"><span className="grid size-12 place-items-center rounded-xl bg-violet-500/10 text-violet-300"><UserRound /></span><div><p className="font-semibold">{data.name}</p><p className="text-sm text-zinc-500">{data.rollNumber}</p></div></div><Detail label="Email" value={data.email} icon={Mail} /><Detail label="Registration" value={data.registrationNumber} /><Detail label="Academic" value={`Semester ${data.semester} · Section ${data.section} · ${data.session}`} /><Detail label="Phone" value={data.phone} icon={Phone} /><Detail label="Status" value={data.status} /></div>}</Modal> }
function Detail({ label, value, icon: Icon }) { return <div className="flex items-center justify-between gap-4 rounded-xl bg-white/[.03] px-4 py-3"><span className="flex items-center gap-2 text-sm text-zinc-500">{Icon && <Icon size={15} />}{label}</span><span className="text-right text-sm capitalize text-zinc-200">{value || '—'}</span></div> }
