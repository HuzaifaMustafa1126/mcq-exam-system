import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { motion } from 'framer-motion'
import { ChevronLeft, ChevronRight, Copy, Eye, FilterX, HelpCircle, Pencil, Plus, Search, Trash2 } from 'lucide-react'
import { useDeferredValue, useState } from 'react'
import toast from 'react-hot-toast'
import Button from '../components/Button'
import Card from '../components/Card'
import Modal from '../components/Modal'
import Skeleton from '../components/Skeleton'
import QuestionFormModal from '../components/questions/QuestionFormModal'
import QuestionPreviewModal from '../components/questions/QuestionPreviewModal'
import { getSubjects } from '../services/subjects'
import { createQuestion, deleteQuestion, getQuestion, getQuestions, updateQuestion } from '../services/questions'

const SIZE = 10; const initialFilters = { subjectId: '', difficulty: '', status: '' }; const date = (value) => value ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium' }).format(new Date(value)) : '—'; const message = (error, fallback) => error.response?.data?.message || fallback

export default function QuestionsPage() {
  const [page, setPage] = useState(1); const [search, setSearch] = useState(''); const [filters, setFilters] = useState(initialFilters); const [formQuestion, setFormQuestion] = useState(undefined); const [previewId, setPreviewId] = useState(null); const [deleteTarget, setDeleteTarget] = useState(null); const deferredSearch = useDeferredValue(search); const client = useQueryClient(); const params = { page, limit: SIZE, search: deferredSearch || undefined, subjectId: filters.subjectId || undefined, difficulty: filters.difficulty || undefined, status: filters.status || undefined }
  const { data, isPending, isError, refetch } = useQuery({ queryKey: ['questions', params], queryFn: () => getQuestions(params) }); const { data: subjectData } = useQuery({ queryKey: ['subjects', 'question-filter'], queryFn: () => getSubjects({ page: 1, limit: 100 }) }); const invalidate = () => client.invalidateQueries({ queryKey: ['questions'] })
  const createMutation = useMutation({ mutationFn: createQuestion, onSuccess: () => { toast.success('Question created successfully'); setFormQuestion(undefined); invalidate() }, onError: (error) => toast.error(message(error, 'Unable to create question')) }); const updateMutation = useMutation({ mutationFn: updateQuestion, onSuccess: () => { toast.success('Question updated successfully'); setFormQuestion(undefined); invalidate() }, onError: (error) => toast.error(message(error, 'Unable to update question')) }); const deleteMutation = useMutation({ mutationFn: deleteQuestion, onSuccess: () => { toast.success('Question deleted successfully'); setDeleteTarget(null); invalidate() }, onError: (error) => toast.error(message(error, 'Unable to delete question')) })
  const questions = data?.questions || []; const pagination = data?.pagination; const filter = (key, value) => { setFilters((current) => ({ ...current, [key]: value })); setPage(1) }; const save = (payload) => formQuestion?.id ? updateMutation.mutate({ id: formQuestion.id, payload }) : createMutation.mutate(payload)
  const duplicate = async (id) => { try { const question = await getQuestion(id); setFormQuestion({ ...question, id: null, questionText: `Copy of ${question.questionText}` }); } catch (error) { toast.error(message(error, 'Unable to duplicate question')) } }
  return <div className="mx-auto max-w-7xl pb-8"><header className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold tracking-wide text-cyan-300">ASSESSMENT CONTENT</p><h1 className="mt-1 text-3xl font-bold tracking-tight sm:text-4xl">Question bank</h1><p className="mt-2 text-sm text-zinc-400 sm:text-base">Create, review, and organize high-quality multiple choice questions.</p></div><Button onClick={() => setFormQuestion({})}><Plus size={18} />Add question</Button></header><Card className="overflow-hidden"><div className="space-y-4 border-b border-white/10 p-5"><div className="flex flex-wrap items-center justify-between gap-4"><div className="relative w-full max-w-xl"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-500" size={18} /><input value={search} onChange={(event) => { setSearch(event.target.value); setPage(1) }} placeholder="Search questions or subjects" className="w-full rounded-xl border border-white/10 bg-white/[.03] py-2.5 pl-10 pr-4 text-sm text-white outline-none placeholder:text-zinc-500 focus:border-cyan-400/60 focus:ring-4 focus:ring-cyan-400/10" /></div><p className="text-sm text-zinc-500">{pagination ? `${pagination.total} questions` : '—'}</p></div><div className="grid gap-3 sm:grid-cols-3"><Filter value={filters.subjectId} onChange={(value) => filter('subjectId', value)} label="All subjects">{subjectData?.subjects?.map((subject) => <option key={subject.id} value={subject.id}>{subject.name}</option>)}</Filter><Filter value={filters.difficulty} onChange={(value) => filter('difficulty', value)} label="All difficulties"><option value="easy">Easy</option><option value="medium">Medium</option><option value="hard">Hard</option></Filter><Filter value={filters.status} onChange={(value) => filter('status', value)} label="All statuses"><option value="active">Active</option><option value="inactive">Inactive</option></Filter></div>{Object.values(filters).some(Boolean) && <button onClick={() => { setFilters(initialFilters); setPage(1) }} className="inline-flex items-center gap-1.5 text-sm text-zinc-400 hover:text-cyan-200"><FilterX size={15} />Clear filters</button>}</div>{isPending ? <Loading /> : isError ? <Error retry={refetch} /> : questions.length === 0 ? <Empty active={Boolean(deferredSearch || Object.values(filters).some(Boolean))} add={() => setFormQuestion({})} /> : <><div className="overflow-x-auto"><table className="w-full min-w-300 text-left text-sm"><thead className="bg-white/[.025] text-xs uppercase tracking-wider text-zinc-500"><tr>{['Question', 'Subject', 'Marks', 'Difficulty', 'Status', 'Created by', 'Created date', 'Actions'].map((heading) => <th key={heading} className="px-5 py-3 font-medium">{heading}</th>)}</tr></thead><tbody>{questions.map((question, index) => <motion.tr key={question.id} initial={{ opacity: 0, y: 6 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.025 }} className="border-t border-white/5"><td className="max-w-110 px-5 py-4"><p className="line-clamp-2 font-medium leading-5 text-white">{question.questionText}</p></td><td className="px-5 py-4 text-zinc-300">{question.subjectName}</td><td className="px-5 py-4">{question.marks}</td><td className="px-5 py-4"><Badge type="difficulty" value={question.difficulty} /></td><td className="px-5 py-4"><Badge type="status" value={question.status} /></td><td className="px-5 py-4 text-zinc-400">{question.createdBy || 'Administrator'}</td><td className="px-5 py-4 text-zinc-400">{date(question.createdAt)}</td><td className="px-5 py-4"><div className="flex gap-1"><Action label="Preview question" onClick={() => setPreviewId(question.id)}><Eye size={16} /></Action><Action label="Duplicate question" onClick={() => duplicate(question.id)}><Copy size={16} /></Action><Action label="Edit question" onClick={async () => { try { setFormQuestion(await getQuestion(question.id)) } catch (error) { toast.error(message(error, 'Unable to load question')) } }}><Pencil size={16} /></Action><Action label="Delete question" danger onClick={() => setDeleteTarget(question)}><Trash2 size={16} /></Action></div></td></motion.tr>)}</tbody></table></div><Pagination data={pagination} setPage={setPage} /></>}</Card><QuestionFormModal open={formQuestion !== undefined} question={formQuestion?.questionText ? formQuestion : null} onClose={() => setFormQuestion(undefined)} onSubmit={save} isPending={createMutation.isPending || updateMutation.isPending} /><QuestionPreviewModal questionId={previewId} onClose={() => setPreviewId(null)} /><DeleteModal question={deleteTarget} close={() => setDeleteTarget(null)} confirm={() => deleteMutation.mutate(deleteTarget.id)} pending={deleteMutation.isPending} /></div>
}

function Filter({ value, onChange, label, children }) { return <select value={value} onChange={(event) => onChange(event.target.value)} className="rounded-xl border border-white/10 bg-white/[.03] px-3 py-2.5 text-sm text-white outline-none focus:border-cyan-400/60"><option value="">{label}</option>{children}</select> }
function Badge({ type, value }) { const colors = type === 'difficulty' ? { easy: 'bg-emerald-500/10 text-emerald-300', medium: 'bg-amber-500/10 text-amber-300', hard: 'bg-rose-500/10 text-rose-300' } : { active: 'bg-cyan-400/10 text-cyan-300', inactive: 'bg-white/10 text-zinc-400' }; return <span className={`rounded-full px-2.5 py-1 text-xs font-medium capitalize ${colors[value]}`}>{value}</span> }
function Action({ children, label, danger, onClick }) { return <button title={label} aria-label={label} onClick={onClick} className={`rounded-lg p-2 transition ${danger ? 'text-rose-300 hover:bg-rose-500/10' : 'text-zinc-400 hover:bg-white/5 hover:text-cyan-200'}`}>{children}</button> }
function Pagination({ data, setPage }) { if (!data || data.totalPages <= 1) return null; return <div className="flex items-center justify-between border-t border-white/10 px-5 py-4"><p className="text-sm text-zinc-500">Page {data.page} of {data.totalPages}</p><div className="flex gap-2"><button disabled={data.page === 1} onClick={() => setPage(data.page - 1)} className="rounded-lg border border-white/10 p-2 disabled:opacity-40"><ChevronLeft size={17} /></button><button disabled={data.page === data.totalPages} onClick={() => setPage(data.page + 1)} className="rounded-lg border border-white/10 p-2 disabled:opacity-40"><ChevronRight size={17} /></button></div></div> }
function Loading() { return <div className="space-y-2 p-5">{Array.from({ length: 7 }, (_, index) => <Skeleton key={index} className="h-17 w-full" />)}</div> }
function Empty({ active, add }) { return <div className="grid min-h-75 place-items-center p-8 text-center"><div><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-cyan-400/10 text-cyan-300"><HelpCircle /></span><h2 className="mt-4 font-bold">{active ? 'No questions found' : 'No questions yet'}</h2><p className="mt-2 text-sm text-zinc-500">{active ? 'Try changing your search or filters.' : 'Create the first question to start building your bank.'}</p>{!active && <Button className="mt-5" onClick={add}><Plus size={16} />Add question</Button>}</div></div> }
function Error({ retry }) { return <div className="p-8 text-center"><p className="font-medium text-rose-300">Unable to load questions.</p><button onClick={retry} className="mt-3 text-sm text-cyan-300">Try again</button></div> }
function DeleteModal({ question, close, confirm, pending }) { return <Modal open={Boolean(question)} onClose={close} title="Delete question"><p className="text-sm leading-6 text-zinc-400">Delete this question? Questions assigned to exams cannot be deleted.</p><div className="mt-6 flex justify-end gap-3"><Button variant="secondary" onClick={close}>Cancel</Button><button onClick={confirm} disabled={pending} className="rounded-xl bg-rose-500 px-5 py-3 text-sm font-semibold text-white disabled:opacity-60">{pending ? 'Deleting...' : 'Delete question'}</button></div></Modal> }
