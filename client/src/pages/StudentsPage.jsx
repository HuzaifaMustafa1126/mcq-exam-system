import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { motion } from 'framer-motion'
import { ChevronLeft, ChevronRight, Eye, FilterX, GraduationCap, Pencil, Plus, Search, Trash2, Users } from 'lucide-react'
import { useDeferredValue, useState } from 'react'
import toast from 'react-hot-toast'
import Button from '../components/Button'
import Card from '../components/Card'
import Modal from '../components/Modal'
import Skeleton from '../components/Skeleton'
import StudentFormModal from '../components/students/StudentFormModal'
import StudentProfileModal from '../components/students/StudentProfileModal'
import { createStudent, deleteStudent, getStudents, updateStudent } from '../services/students'

const PAGE_SIZE = 10
const formatDate = (value) => value ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium' }).format(new Date(value)) : '—'
const message = (error, fallback) => error.response?.data?.message || fallback
const initialFilters = { semester: '', section: '', status: '' }

export default function StudentsPage() {
  const [page, setPage] = useState(1); const [search, setSearch] = useState(''); const [filters, setFilters] = useState(initialFilters); const [formStudent, setFormStudent] = useState(undefined); const [detailsId, setDetailsId] = useState(null); const [deleteTarget, setDeleteTarget] = useState(null)
  const deferredSearch = useDeferredValue(search); const queryClient = useQueryClient(); const params = { page, limit: PAGE_SIZE, search: deferredSearch || undefined, semester: filters.semester || undefined, section: filters.section || undefined, status: filters.status || undefined }
  const { data, isPending, isError, refetch } = useQuery({ queryKey: ['students', params], queryFn: () => getStudents(params) }); const invalidate = () => queryClient.invalidateQueries({ queryKey: ['students'] })
  const createMutation = useMutation({ mutationFn: createStudent, onSuccess: () => { toast.success('Student created successfully'); setFormStudent(undefined); invalidate() }, onError: (error) => toast.error(message(error, 'Unable to create student')) }); const updateMutation = useMutation({ mutationFn: updateStudent, onSuccess: () => { toast.success('Student updated successfully'); setFormStudent(undefined); invalidate() }, onError: (error) => toast.error(message(error, 'Unable to update student')) }); const deleteMutation = useMutation({ mutationFn: deleteStudent, onSuccess: () => { toast.success('Student deleted successfully'); setDeleteTarget(null); invalidate() }, onError: (error) => toast.error(message(error, 'Unable to delete student')) })
  const students = data?.students || []; const pagination = data?.pagination; const updateFilter = (field, value) => { setFilters((current) => ({ ...current, [field]: value })); setPage(1) }
  const save = (payload) => formStudent?.id ? updateMutation.mutate({ id: formStudent.id, payload }) : createMutation.mutate(payload)
  return <div className="mx-auto max-w-7xl pb-8"><header className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold tracking-wide text-cyan-300">PEOPLE MANAGEMENT</p><h1 className="mt-1 text-3xl font-bold tracking-tight sm:text-4xl">Students</h1><p className="mt-2 text-sm text-zinc-400 sm:text-base">Manage student accounts, academic profiles, and access status.</p></div><Button onClick={() => setFormStudent({})}><Plus size={18} />Add student</Button></header><Card className="overflow-hidden"><div className="space-y-4 border-b border-white/10 p-5"><div className="flex flex-wrap items-center justify-between gap-4"><div className="relative w-full max-w-md"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-500" size={18} /><input value={search} onChange={(event) => { setSearch(event.target.value); setPage(1) }} placeholder="Search name, email, or student number" className="w-full rounded-xl border border-white/10 bg-white/[.03] py-2.5 pl-10 pr-4 text-sm text-white outline-none transition placeholder:text-zinc-500 focus:border-cyan-400/60 focus:ring-4 focus:ring-cyan-400/10" /></div><p className="text-sm text-zinc-500">{pagination ? `${pagination.total} students` : '—'}</p></div><div className="grid gap-3 sm:grid-cols-3"><FilterSelect value={filters.semester} onChange={(value) => updateFilter('semester', value)} label="All semesters">{Array.from({ length: 20 }, (_, index) => <option key={index + 1} value={index + 1}>Semester {index + 1}</option>)}</FilterSelect><input value={filters.section} onChange={(event) => updateFilter('section', event.target.value)} placeholder="Filter section (e.g. A)" className="rounded-xl border border-white/10 bg-white/[.03] px-3 py-2.5 text-sm text-white outline-none placeholder:text-zinc-500 focus:border-cyan-400/60" /><FilterSelect value={filters.status} onChange={(value) => updateFilter('status', value)} label="All statuses"><option value="active">Active</option><option value="inactive">Inactive</option><option value="suspended">Suspended</option></FilterSelect></div>{Object.values(filters).some(Boolean) && <button onClick={() => { setFilters(initialFilters); setPage(1) }} className="inline-flex items-center gap-1.5 text-sm text-zinc-400 hover:text-cyan-200"><FilterX size={15} />Clear filters</button>}</div>{isPending ? <TableSkeleton /> : isError ? <ErrorState retry={refetch} /> : students.length === 0 ? <Empty search={deferredSearch || Object.values(filters).some(Boolean)} add={() => setFormStudent({})} /> : <><div className="overflow-x-auto"><table className="w-full min-w-275 text-left text-sm"><thead className="bg-white/[.025] text-xs uppercase tracking-wider text-zinc-500"><tr>{['Student number', 'Student', 'Semester', 'Section', 'Session', 'Status', 'Created date', 'Actions'].map((heading) => <th key={heading} className="px-5 py-3 font-medium">{heading}</th>)}</tr></thead><tbody>{students.map((student, index) => <motion.tr key={student.id} initial={{ opacity: 0, y: 6 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.03 }} className="border-t border-white/5 text-zinc-300"><td className="px-5 py-4 font-mono text-xs text-cyan-200">{student.rollNumber}</td><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg bg-violet-500/10 text-violet-300"><GraduationCap size={17} /></span><div><p className="font-medium text-white">{student.name}</p><p className="text-xs text-zinc-500">{student.email}</p></div></div></td><td className="px-5 py-4">{student.semester}</td><td className="px-5 py-4">{student.section}</td><td className="px-5 py-4">{student.session}</td><td className="px-5 py-4"><Status status={student.status} /></td><td className="px-5 py-4 text-zinc-400">{formatDate(student.createdAt)}</td><td className="px-5 py-4"><div className="flex gap-1"><Action label="View student" onClick={() => setDetailsId(student.id)}><Eye size={16} /></Action><Action label="Edit student" onClick={() => setFormStudent(student)}><Pencil size={16} /></Action><Action label="Delete student" danger onClick={() => setDeleteTarget(student)}><Trash2 size={16} /></Action></div></td></motion.tr>)}</tbody></table></div><Pager pagination={pagination} onPage={setPage} /></>}</Card><StudentFormModal open={formStudent !== undefined} student={formStudent?.id ? formStudent : null} onClose={() => setFormStudent(undefined)} onSubmit={save} isPending={createMutation.isPending || updateMutation.isPending} /><StudentProfileModal studentId={detailsId} onClose={() => setDetailsId(null)} /><DeleteModal student={deleteTarget} close={() => setDeleteTarget(null)} confirm={() => deleteMutation.mutate(deleteTarget.id)} pending={deleteMutation.isPending} /></div>
}

function FilterSelect({ value, onChange, label, children }) { return <select value={value} onChange={(event) => onChange(event.target.value)} className="rounded-xl border border-white/10 bg-white/[.03] px-3 py-2.5 text-sm text-white outline-none focus:border-cyan-400/60"><option value="">{label}</option>{children}</select> }
function Status({ status }) { const color = { active: 'bg-emerald-500/10 text-emerald-300', inactive: 'bg-amber-500/10 text-amber-300', suspended: 'bg-rose-500/10 text-rose-300' }[status] || 'bg-white/10'; return <span className={`rounded-full px-2.5 py-1 text-xs font-medium capitalize ${color}`}>{status}</span> }
function Action({ children, label, danger, onClick }) { return <button title={label} aria-label={label} onClick={onClick} className={`rounded-lg p-2 transition ${danger ? 'text-rose-300 hover:bg-rose-500/10' : 'text-zinc-400 hover:bg-white/5 hover:text-cyan-200'}`}>{children}</button> }
function Pager({ pagination, onPage }) { if (!pagination || pagination.totalPages <= 1) return null; return <div className="flex items-center justify-between border-t border-white/10 px-5 py-4"><p className="text-sm text-zinc-500">Page {pagination.page} of {pagination.totalPages}</p><div className="flex gap-2"><button onClick={() => onPage(pagination.page - 1)} disabled={pagination.page === 1} className="rounded-lg border border-white/10 p-2 disabled:opacity-40"><ChevronLeft size={17} /></button><button onClick={() => onPage(pagination.page + 1)} disabled={pagination.page === pagination.totalPages} className="rounded-lg border border-white/10 p-2 disabled:opacity-40"><ChevronRight size={17} /></button></div></div> }
function TableSkeleton() { return <div className="space-y-2 p-5">{Array.from({ length: 6 }, (_, index) => <Skeleton key={index} className="h-16 w-full" />)}</div> }
function Empty({ search, add }) { return <div className="grid min-h-75 place-items-center p-8 text-center"><div><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-cyan-400/10 text-cyan-300"><Users /></span><h2 className="mt-4 font-bold">{search ? 'No students found' : 'No students yet'}</h2><p className="mt-2 text-sm text-zinc-500">{search ? 'Try changing your search or filters.' : 'Create the first student account to get started.'}</p>{!search && <Button className="mt-5" onClick={add}><Plus size={16} />Add student</Button>}</div></div> }
function ErrorState({ retry }) { return <div className="p-8 text-center"><p className="font-medium text-rose-300">Unable to load students.</p><button onClick={retry} className="mt-3 text-sm text-cyan-300">Try again</button></div> }
function DeleteModal({ student, close, confirm, pending }) { return <Modal open={Boolean(student)} onClose={close} title="Delete student"><p className="text-sm leading-6 text-zinc-400">Delete <span className="font-medium text-white">{student?.name}</span>? This removes their account and cannot be undone.</p><div className="mt-6 flex justify-end gap-3"><Button variant="secondary" onClick={close}>Cancel</Button><button onClick={confirm} disabled={pending} className="rounded-xl bg-rose-500 px-5 py-3 text-sm font-semibold text-white disabled:opacity-60">{pending ? 'Deleting...' : 'Delete student'}</button></div></Modal> }
