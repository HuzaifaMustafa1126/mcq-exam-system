import { useQuery } from '@tanstack/react-query'
import { Eye, Search } from 'lucide-react'
import { useDeferredValue, useState } from 'react'
import Card from '../components/Card'
import ResultDetailsModal from '../components/results/ResultDetailsModal'
import { getResults } from '../services/results'

export default function TeacherResultsPage() { const [search, setSearch] = useState(''); const [detailsId, setDetailsId] = useState(null); const deferred = useDeferredValue(search); const { data, isPending } = useQuery({ queryKey: ['teacher-results', deferred], queryFn: () => getResults({ page: 1, limit: 100, search: deferred || undefined }) }); const results = data?.results || []; return <div className="mx-auto max-w-7xl"><p className="text-sm font-semibold text-cyan-300">EXAM RESULTS</p><h1 className="mt-1 text-3xl font-bold">Student performance</h1><p className="mt-2 text-zinc-400">Review outcomes for examinations you created.</p><Card className="mt-7 overflow-hidden"><div className="border-b border-white/10 p-5"><div className="relative max-w-lg"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-500" size={18} /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search student results" className="w-full rounded-xl border border-white/10 bg-white/[.03] py-2.5 pl-10 pr-4 text-sm text-white outline-none" /></div></div>{isPending ? <p className="p-8 text-zinc-500">Loading results...</p> : <div className="overflow-x-auto"><table className="w-full min-w-200 text-left text-sm"><thead className="bg-white/[.025] text-xs uppercase text-zinc-500"><tr>{['Student', 'Exam', 'Score', 'Percentage', 'Result', 'Action'].map((heading) => <th key={heading} className="px-5 py-3">{heading}</th>)}</tr></thead><tbody>{results.map((result) => <tr key={result.attemptId} className="border-t border-white/5"><td className="px-5 py-4"><p className="font-medium text-white">{result.studentName}</p><p className="text-xs text-zinc-500">{result.studentNumber}</p></td><td className="px-5 py-4">{result.examName}</td><td className="px-5 py-4">{result.obtainedMarks}/{result.totalMarks}</td><td className="px-5 py-4">{result.percentage}%</td><td className="px-5 py-4"><span className={result.status === 'PASS' ? 'text-emerald-300' : 'text-rose-300'}>{result.status}</span></td><td className="px-5 py-4"><button onClick={() => setDetailsId(result.attemptId)} className="rounded-lg p-2 text-cyan-300 hover:bg-white/5"><Eye size={17} /></button></td></tr>)}</tbody></table></div>}</Card><ResultDetailsModal attemptId={detailsId} onClose={() => setDetailsId(null)} /></div> }
